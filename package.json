{
  "name": "snack-bar",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter client test:e2e",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
